#include <stdio.h>

int main()
{
    // 3.0 + 1 will be:
    // a. Integer.
    // b. Floating point number.
    // c. Character.

    // Answer is b, because 3.0 is a floating point number and any operation with float will return float
    
    return 0;
}