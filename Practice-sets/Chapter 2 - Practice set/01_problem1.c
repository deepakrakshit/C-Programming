#include <stdio.h>

int main()
{
    // Which of the following is invalid in C ? 
    // a. int a = 1, int b = a;
    // b. int v = 3 * 3;
    // c. char dt = ‘21 dec 2020’;

    // answer is c, because char can only have 1 word

    return 0;
}