// Twenty integers are to be stored in memory. What will you prefer- Array or structure?

// i will prefer array because all are in same types and 20 integers are not a big deal to handle, as arrays have only 1 line syntax